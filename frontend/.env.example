# Frontend Environment Variables (Development Only)
# These variables are used during local development with Vite

# Backend API URL for development proxy (optional)
# Default: http://localhost:3001
# VITE_API_URL=http://localhost:3001

# Note: In production (Vercel), API requests are proxied via vercel.json
# No environment variables are needed for production frontend deployment
